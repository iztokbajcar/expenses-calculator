version: "3.8"

services:
  apache: 
    build: "./apache/"
    depends_on:
      - php
      - mysql
    networks:
      - frontend
      - backend
    ports:
      - "8080:80"
    volumes:
      - ./web/:/var/www/html/
  
  mysql:
    image: mysql:5.6.40
    networks:
      - backend
    environment:
      - MYSQL_ROOT_PASSWORD=Privz3toGesl0
  
  networks:
    frontend:
    backend:
